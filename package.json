{
  "name": "project",
  "version": "1.0.0",
  "description": "An autonomous AI agent that searches the web, reads webpages, executes Python code, and synthesizes comprehensive answers with a streaming UI.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,green\" \"npm run backend\" \"npm run frontend\"",
    "backend": "backend/venv/bin/uvicorn main:app --reload --port 8000 --app-dir backend",
    "frontend": "cd frontend && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
